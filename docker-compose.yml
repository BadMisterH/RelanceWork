services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      # Persister la base de données SQLite en dehors du conteneur
      - ./data:/app/data
    environment:
      - NODE_ENV=production
    restart: unless-stopped
